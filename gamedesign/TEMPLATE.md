# <System Name>

*Status: PROVEN / REFINING / CANDIDATE. One line on how settled it is.*

## The idea
What this system is *for* — the experience/feeling it creates, why it exists, what it's the
answer to. (The pitch, before any mechanics.)

## How it works
The actual mechanics, step by step. State it plainly enough that it could be rebuilt from this
alone. Include:
- **State it owns** — db attributes/flags, where they live, defaults, what resets them.
- **The core loop / flow** — what happens, in what order, with what thresholds/tuning.
- **Edge cases & failure modes** — what was tricky, what broke, what the fix was.

## Commands & verbs
Player-facing: `<cmd>` — what it does. Builder/admin: `<cmd>` — what it does.
(Note aliases, locks, and which cmdset.)

## Real subsystems it uses / routes through
What existing systems it calls (never fake — list the real functions/typeclasses).

## Tie-ins with other systems
How it connects to / could connect to other systems (inputs it reads, state it sets that
others react to, scenes/events that hook it). Note synergies worth keeping together.

## §0 OOC floor
How the always-available escape/reset clears every bit of this system's state. (Non-negotiable.)

## What works / what doesn't (the trial-by-error log)
- ✅ Kept: …
- ✂️ Cut / didn't land: …
- 🔧 Tuning notes for the fresh build: …

## Open questions / future
Anything unresolved, or directions to consider when rebuilding clean.
